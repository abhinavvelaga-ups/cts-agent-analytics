// ─────────────────────────────────────────────────────────────
// Dashboard configuration — edit these two lines for your repo.
// The dashboard auto-discovers every file in the data/ folder via the
// GitHub API (so new developers appear automatically). If the API is
// unavailable (e.g. opening index.html locally), it falls back to
// data/manifest.json.
// ─────────────────────────────────────────────────────────────
window.DASHBOARD_CONFIG = {
  // Your GitHub repo that hosts this dashboard, as "owner/repo".
  githubRepo: "abhinavvelaga-ups/cts-agent-analytics",

  // Branch the GitHub Pages site is served from (usually "main" or "gh-pages").
  githubBranch: "main",

  // Folder (inside the repo) holding the per-developer JSON files.
  // The dashboard is published from the repo ROOT, so data lives at "data".
  dataDir: "data",
};
